///////////////////////////////////////////////////////////////////////////////////////////////
////////////////////////////////////// Toast Function ////////////////////////////////////////
/////////////////////////////////////////////////////////////////////////////////////////////

function toast(priority,title,message,timeout=5000,) {
    $.toaster({ priority :priority, title :title, settings : {'timeout': timeout}, message :message});
}